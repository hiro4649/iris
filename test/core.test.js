// Kept for future migration to Node's built-in test runner.
// The current sandbox blocks node --test child process spawning, so scripts/run-tests.js
// is the authoritative local test runner for now.
